# 🏗️ Architecture Diagram - Investment Data Stream Service

> **Примечание**: Этот документ описывает текущую модульную архитектуру сервиса. Для подробного описания новой архитектуры см. [NEW_STREAMING_ARCHITECTURE.md](NEW_STREAMING_ARCHITECTURE.md)

## 📊 System Overview

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Tinkoff Invest API (gRPC Streams)                            │
└──────────────────────┬──────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│              Investment Data Stream Service                                     │
│                                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │ Trade Stream │  │ Candle Stream│  │ LastPrice    │  │ Limit Monitor│        │
│  │              │  │              │  │ Stream       │  │ Stream       │        │
│  │ Controller   │  │ Controller   │  │ Controller   │  │ Controller   │        │
│  │ Processor    │  │ Processor    │  │ Processor    │  │ Monitor      │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│         │                 │                  │                  │               │
│         ▼                 ▼                  ▼                  ▼               │
│  ┌─────────────────────────────────────────────────────────────────────┐       │
│  │              PostgreSQL Database                                     │       │
│  │  invest.trades | invest.minute_candles | invest.last_prices         │       │
│  └─────────────────────────────────────────────────────────────────────┘       │
│                                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                          │
│  │ Cache       │  │ Instrument   │  │ Telegram     │                          │
│  │ Service     │  │ Service      │  │ Bot          │                          │
│  │             │  │              │  │              │                          │
│  │ sharesCache│  │ /api/        │  │ Notifications│                          │
│  │ futuresCache│ │ instruments/*│  │ Commands     │                          │
│  │ indicatives │  │              │  │              │                          │
│  │ limitsCache │  │              │  │              │                          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                          │
└─────────┼──────────────────┼──────────────────┼──────────────────────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│              REST API                                                           │
│  /api/stream/trades/* | /api/stream/minute-candles/* |                         │
│  /api/stream/last-price/* | /api/stream/limits/* |                            │
│  /api/cache/* | /api/instruments/*                                             │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🎯 Data Flow

### 1. **Instrument Loading & Caching**

```
CacheWarmupService → getAllInstruments() → shares + futures + indicatives → Cache
```

### 2. **Stream Subscription Setup**

```
Each Stream Controller → GrpcConnectionManager → T-Invest API (gRPC Stream)
```

### 3. **Independent Stream Processing**

```
Trade Stream:
  T-Invest API → TradeStreamingService → TradeProcessor → invest.trades

MinuteCandle Stream:
  T-Invest API → MinuteCandleStreamingService → CandleProcessor → invest.minute_candles

LastPrice Stream:
  T-Invest API → LastPriceStreamingService → LastPriceProcessor → invest.last_prices

Limit Monitoring Stream:
  LastPrice Stream → LimitMonitorService → Telegram Notifications
```

### 4. **Database Storage**

Каждый стрим пишет в свою таблицу:
- `invest.trades` - обезличенные сделки
- `invest.minute_candles` - минутные свечи
- `invest.last_prices` - цены последних сделок

## ⚡ Performance Architecture

### **Thread Pools:**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              Thread Configuration                               │
│                                                                                 │
│  Trade Insert Threads: CPU_CORES * 6                                           │
│  Max Concurrent Inserts: 200                                                   │
│  Semaphore Control: tradeInsertSemaphore                                       │
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │   Executor      │    │   Semaphore     │    │   Batch         │            │
│  │   Service       │    │   Control       │    │   Processing    │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### **Database Optimization:**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            Database Layer                                       │
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │   UPSERT        │    │   Batch         │    │   Indexes       │            │
│  │   Operations    │    │   Processing    │    │   Optimization  │            │
│  │                 │    │                 │    │                 │            │
│  │ ON CONFLICT     │    │ 100+ records    │    │ figi, time,     │            │
│  │ DO UPDATE       │    │ per batch       │    │ direction       │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Reconnection Logic

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            Reconnection Flow                                    │
│                                                                                 │
│  Error/Disconnect → scheduleReconnect() → Exponential Backoff → Restart Stream │
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │   Error         │    │   Delay         │    │   Restart       │            │
│  │   Detection     │    │   Calculation   │    │   Stream        │            │
│  │                 │    │                 │    │                 │            │
│  │ onError()       │    │ 1s, 2s, 4s...   │    │ startLastPrice  │            │
│  │ onCompleted()   │    │ Max: 30s        │    │ Stream()        │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 📊 Monitoring & Statistics

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            Service Statistics                                   │
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │   Trade         │    │   Price         │    │   Error         │            │
│  │   Metrics       │    │   Metrics       │    │   Metrics       │            │
│  │                 │    │                 │    │                 │            │
│  │ • Processed     │    │ • Received      │    │ • Total Errors  │            │
│  │ • Errors        │    │ • Last Price    │    │ • Error Rate    │            │
│  │ • Received      │    │ • Trade Data    │    │ • Reconnections │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🎯 Key Components

### **Streaming Services:**

- `TradeStreamingService` - Обработка обезличенных сделок
- `MinuteCandleStreamingService` - Обработка минутных свечей
- `LastPriceStreamingService` - Обработка цен последних сделок
- `LimitMonitoringStreamingService` - Мониторинг лимитов
- `GrpcConnectionManager` - Управление gRPC подключениями

### **Controllers:**

- `TradeStreamController` - `/api/stream/trades/*`
- `MinuteCandleStreamController` - `/api/stream/minute-candles/*`
- `LastPriceStreamController` - `/api/stream/last-price/*`
- `LimitStreamController` - `/api/stream/limits/*`
- `CacheController` - `/api/cache/*`
- `InstrumentController` - `/api/instruments/*`

### **Processors:**

- `TradeProcessor` - Обработка сделок с асинхронной записью в БД
- `CandleProcessor` - Обработка свечей с расчетом технических показателей
- `LastPriceProcessor` - Обработка цен последних сделок
- `LimitMonitorService` - Мониторинг приближения к лимитам

### **Entities:**

- `TradeEntity` + `TradeKey` - Обезличенные сделки
- `MinuteCandleEntity` + `MinuteCandleKey` - Минутные свечи
- `LastPriceEntity` + `LastPriceKey` - Цены последних сделок
- `ShareEntity` - Справочные данные акций
- `FutureEntity` - Справочные данные фьючерсов
- `IndicativeEntity` - Справочные данные индикативных инструментов

### **Services:**

- `CacheWarmupService` - Управление кэшем инструментов
- `LimitsService` - Работа с лимитами инструментов
- `TgBotService` - Telegram бот для мониторинга и уведомлений

### **Configuration:**

- `GrpcConfig` - gRPC client configuration
- `CacheConfig` - Конфигурация кэшей (Caffeine)
- `SchedulingConfig` - Конфигурация планировщика задач
- Application properties for different environments
- SQL scripts for database setup

## 📊 Преимущества новой архитектуры

1. **Независимость стримов**: Каждый стрим работает независимо
2. **Модульность**: Легко добавить новый тип стрима
3. **Масштабируемость**: Каждый стрим можно масштабировать отдельно
4. **Мониторинг**: Детальные метрики для каждого стрима
5. **Надежность**: Падение одного стрима не влияет на другие
